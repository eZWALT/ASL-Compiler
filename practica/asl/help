func extrafunc(a : array[2] of int)

	write a[0];
	a[0] = 5;
	write a[0];
	return;
endfunc

func main()
	var a : array [2] of int

	a[0] = 0;
	a[1] = 1;
	
	extrafunc(a);
	write a[0];
	write a[1];
	return;
endfunc
